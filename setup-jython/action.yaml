name: coatl-dev-setup-jython
description: Set up a specific version of Jython and add the command-line tools to the PATH.
author: cesarcoatl

inputs:
  jython-version:
    description: >-
      The Jython version to install.
    required: false
    default: '2.7.4'
  java-distribution:
    description: >-
      Java distribution.
    required: false
    default: 'temurin'
  java-version:
    description: >-
      The Java version to set up.
    required: false
    default: '17'
outputs:
  jython-version:
    description: >-
      The installed Jython version.
    value: ${{ steps.get-version.outputs.version }}
  jython-path:
    description: >-
      The absolute path to the Jython executable.
    value: ${{ steps.get-path.outputs.path }}
  java-distribution:
    description: >-
      Distribution of Java that has been installed.
    value: ${{ steps.get-java-distribution.outputs.java-distribution }}
  java-version:
    description: >-
      Actual version of the java environment that has been installed.
    value: ${{ steps.get-java-version.outputs.java-version }}
  java-path:
    description: >-
      Path to where the java environment has been installed (same as $JAVA_HOME).
    value: ${{ steps.get-java-path.outputs.java-path }}

runs:
  using: composite
  steps:
    - name: Set Jython installation path
      shell: bash
      env:
        JYTHON_VERSION: ${{ inputs.jython-version }}
      run: | # zizmor: ignore[github-env]
        echo "JYTHON_PATH=/opt/jython/${JYTHON_VERSION}" >> "$GITHUB_ENV"

    - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
      id: setup-java
      with:
        distribution: ${{ inputs.java-distribution }}
        java-version: ${{ inputs.java-version }}

    - name: Cache Jython
      id: cache-jython
      uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: ${{ env.JYTHON_PATH }}
        key: jython-${{ inputs.jython-version }}-${{ runner.os }}-${{ steps.setup-java.outputs.distribution }}-${{ steps.setup-java.outputs.version }}

    - name: Install Jython
      if: steps.cache-jython.outputs.cache-hit != 'true'
      shell: bash
      env:
        JYTHON_VERSION: ${{ inputs.jython-version }}
      run: |
        wget -q "https://repo1.maven.org/maven2/org/python/jython-installer/${JYTHON_VERSION}/jython-installer-${JYTHON_VERSION}.jar"
        java -jar "jython-installer-${JYTHON_VERSION}.jar" \
          --silent \
          --type standard \
          --directory "$JYTHON_PATH"
        rm -f "jython-installer-${JYTHON_VERSION}.jar"

    - name: Add Jython to PATH
      shell: bash
      run: | # zizmor: ignore[github-env]
        echo "$JYTHON_PATH/bin" >> $GITHUB_PATH

    - name: Get Jython version
      id: get-version
      shell: bash
      run: |
        jython --version 2>.jython-version
        output=$(sed "s/Jython //" ".jython-version")
        rm -f .jython-version
        echo "version=$output" >> "$GITHUB_OUTPUT"

    - name: Get absolute path
      id: get-path
      shell: bash
      run: |
        echo "path=$(which jython)" >> "$GITHUB_OUTPUT"

    - name: Set JYTHON_HOME
      shell: bash
      run: | # zizmor: ignore[github-env]
        echo "JYTHON_HOME=$(dirname $(dirname $(which jython)))" >> "$GITHUB_ENV"

    - name: Get Java distribution
      id: get-java-distribution
      shell: bash
      run: |
        echo "java-distribution=${STEPS_SETUP_JAVA_OUTPUTS_DISTRIBUTION}" >> "$GITHUB_OUTPUT"
      env:
        STEPS_SETUP_JAVA_OUTPUTS_DISTRIBUTION: ${{ steps.setup-java.outputs.distribution }}

    - name: Get Java path
      id: get-java-path
      shell: bash
      run: |
        echo "java-path=${STEPS_SETUP_JAVA_OUTPUTS_PATH}" >> "$GITHUB_OUTPUT"
      env:
        STEPS_SETUP_JAVA_OUTPUTS_PATH: ${{ steps.setup-java.outputs.path }}

    - name: Get Java version
      id: get-java-version
      shell: bash
      run: |
        echo "java-version=${STEPS_SETUP_JAVA_OUTPUTS_VERSION}" >> "$GITHUB_OUTPUT"
      env:
        STEPS_SETUP_JAVA_OUTPUTS_VERSION: ${{ steps.setup-java.outputs.version }}
